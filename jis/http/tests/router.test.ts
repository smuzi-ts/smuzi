import { assert, describe, it, okMsg } from "@jis/tests";

type Route = {
    method: string,
    path: string
}

describe("Std-router", () => {
    it(okMsg("Matched to Some"), () => {
        let routes = new Map([

        ])
    })
})